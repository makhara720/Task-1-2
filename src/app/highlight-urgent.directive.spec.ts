import { HighlightUrgentDirective } from './highlight-urgent.directive';

describe('HighlightUrgentDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightUrgentDirective();
    expect(directive).toBeTruthy();
  });
});
